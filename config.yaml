# Default training configuration. CLI args override these values.

data_dir: /path/to/your/data
in_channels: 10
out_channels: 1
batch_size: 32
learning_rate: 0.001
max_epochs: 100
num_workers: 4
seed: 42
output_dir: outputs
